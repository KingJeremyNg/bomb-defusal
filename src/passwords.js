const passwordsInput = document.getElementById('passwordsInput');
const passwordsOutput = document.getElementById('passwordsOutput');

passwordsInput.onkeydown = keyPress;

function keyPress(e) {
    // console.log(passwordsInput.value);
}
